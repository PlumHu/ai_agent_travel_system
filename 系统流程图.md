# AI Agent 旅行规划系统 - 完整流程图

## 1. 系统总体架构流程图

```mermaid
graph TB
    User[用户请求] --> AgentManager[Agent Manager<br/>智能路由]

    AgentManager --> RecommendAgent[推荐 Agent]
    AgentManager --> PlanAgent[规划 Agent]
    AgentManager --> BrowserAgent[浏览器 Agent]

    subgraph LLM层
        LLMConfig[LLM 配置管理器]
        LLMConfig --> BaiduOneAPI[百度 OneAPI]
        LLMConfig --> NvidiaAPI[英伟达 NIM]
        LLMConfig --> DeepSeek[DeepSeek API]
        LLMConfig --> OpenAI[OpenAI API]
        LLMConfig --> CustomAPI[自定义接口]
    end

    RecommendAgent --> LLMConfig
    PlanAgent --> LLMConfig
    BrowserAgent --> LLMConfig

    subgraph 工具层
        WeatherTool[天气工具]
        MapTool[地图工具]
        SearchTool[搜索工具]

        WeatherTool --> WeatherMCP[OpenWeather MCP]
        WeatherTool --> WeatherMock[模拟天气数据]

        MapTool --> BaiduMapsMCP[百度地图 MCP]
        MapTool --> MapMock[模拟地图数据]

        SearchTool --> DuckDuckGo[DuckDuckGo<br/>免费搜索]
        SearchTool --> BraveSearch[Brave Search<br/>可选]
        SearchTool --> BrowserSearch[Browser-Use<br/>兜底]
        SearchTool --> SearchMock[模拟搜索数据]
    end

    RecommendAgent --> WeatherTool
    RecommendAgent --> MapTool
    RecommendAgent --> SearchTool

    BrowserAgent --> WebBrowser[Chromium<br/>Playwright]

    WeatherMCP --> ExternalAPI1[OpenWeather API]
    BaiduMapsMCP --> ExternalAPI2[百度地图 API]
    DuckDuckGo --> ExternalAPI3[DuckDuckGo]

    RecommendAgent --> Result[返回结果]
    PlanAgent --> Result
    BrowserAgent --> Result
    Result --> User

    style LLMConfig fill:#e1f5ff
    style DuckDuckGo fill:#c8e6c9
    style WeatherMCP fill:#fff9c4
    style BaiduMapsMCP fill:#fff9c4
    style BrowserAgent fill:#ffccbc
```

---

## 2. LLM 配置选择流程图

```mermaid
graph LR
    Start[创建 Agent] --> CheckProvider{指定 LLM<br/>提供商?}

    CheckProvider -->|是| UseProvider[使用指定提供商]
    CheckProvider -->|否| CheckEnv{环境变量<br/>DEFAULT_LLM_PROVIDER?}

    CheckEnv -->|有| UseEnvProvider[使用环境变量提供商]
    CheckEnv -->|无| AutoDetect[自动检测可用提供商]

    UseProvider --> CheckAPIKey{API Key<br/>已配置?}
    UseEnvProvider --> CheckAPIKey
    AutoDetect --> CheckAPIKey

    CheckAPIKey -->|是| CreateClient[创建 OpenAI 客户端]
    CheckAPIKey -->|否| Error[抛出错误:<br/>未设置 API Key]

    CreateClient --> InitAgent[Agent 初始化完成]

    InitAgent --> ReadyToUse[可以使用]

    style CheckProvider fill:#e1f5ff
    style CheckAPIKey fill:#fff9c4
    style Error fill:#ffcdd2
    style ReadyToUse fill:#c8e6c9
```

---

## 3. 搜索工具智能降级流程图

```mermaid
graph TB
    SearchRequest[搜索请求] --> TryDuckDuckGo{尝试<br/>DuckDuckGo}

    TryDuckDuckGo -->|成功| ReturnDuckDuckGo[返回 DuckDuckGo<br/>搜索结果]
    TryDuckDuckGo -->|失败| CheckBrave{Brave Search<br/>已配置?}

    CheckBrave -->|是| TryBrave{尝试<br/>Brave Search}
    CheckBrave -->|否| CheckBrowser{允许使用<br/>浏览器兜底?}

    TryBrave -->|成功| ReturnBrave[返回 Brave<br/>搜索结果]
    TryBrave -->|失败| CheckBrowser

    CheckBrowser -->|是| TryBrowser{尝试<br/>Browser-Use}
    CheckBrowser -->|否| ReturnMock[返回模拟数据]

    TryBrowser -->|成功| ReturnBrowser[返回浏览器<br/>搜索结果]
    TryBrowser -->|失败| ReturnMock

    ReturnDuckDuckGo --> End[返回给 Agent]
    ReturnBrave --> End
    ReturnBrowser --> End
    ReturnMock --> End

    style ReturnDuckDuckGo fill:#c8e6c9
    style ReturnBrave fill:#fff9c4
    style ReturnBrowser fill:#ffccbc
    style ReturnMock fill:#e0e0e0
```

---

## 4. BrowserAgent 执行流程图

```mermaid
sequenceDiagram
    participant User as 用户
    participant Agent as BrowserAgent
    participant LLM as LLM API
    participant Browser as Chromium
    participant Web as 目标网站

    User->>Agent: 提交任务<br/>("搜索北京旅游景点")

    Agent->>Agent: 初始化 LLM 客户端
    Agent->>Browser: 启动浏览器<br/>(无头模式)
    Browser-->>Agent: 浏览器就绪

    Agent->>Browser: 导航到起始 URL<br/>(https://www.baidu.com)
    Browser->>Web: HTTP 请求
    Web-->>Browser: 返回页面
    Browser-->>Agent: 页面加载完成

    loop 最多 30 步
        Agent->>LLM: 发送页面状态 + 任务描述
        LLM-->>Agent: 返回下一步操作<br/>(点击/输入/提取)

        Agent->>Browser: 执行操作<br/>(输入搜索词)
        Browser->>Web: 交互操作
        Web-->>Browser: 页面更新
        Browser-->>Agent: 操作完成

        Agent->>Agent: 检查任务是否完成
    end

    Agent->>Browser: 提取结果
    Browser-->>Agent: 返回页面数据

    Agent->>Browser: 关闭浏览器
    Browser-->>Agent: 浏览器已关闭

    Agent->>User: 返回结果
```

---

## 5. 旅行推荐 Agent 完整流程图

```mermaid
graph TB
    UserInput[用户输入<br/>"推荐3月旅游目的地"] --> ParseIntent[解析用户意图]

    ParseIntent --> ExtractInfo[提取关键信息:<br/>- 时间: 3月<br/>- 偏好: 未指定]

    ExtractInfo --> SearchDest{搜索目的地}

    SearchDest --> TrySearch1[尝试 DuckDuckGo 搜索]
    TrySearch1 -->|成功| GetSearchResults[获取候选目的地]
    TrySearch1 -->|失败| TrySearch2[尝试 Brave Search]
    TrySearch2 -->|成功| GetSearchResults
    TrySearch2 -->|失败| TrySearch3[浏览器搜索]
    TrySearch3 --> GetSearchResults

    GetSearchResults --> CheckWeather{查询天气信息}

    CheckWeather --> TryWeather1[尝试 OpenWeather MCP]
    TryWeather1 -->|成功| GetWeatherData[获取天气数据]
    TryWeather1 -->|失败| UseWeatherMock[使用历史天气数据]
    UseWeatherMock --> GetWeatherData

    GetWeatherData --> QueryMap{查询地点信息}

    QueryMap --> TryMap1[尝试百度地图 MCP]
    TryMap1 -->|成功| GetMapData[获取地点详情]
    TryMap1 -->|失败| UseMapMock[使用模拟地点数据]
    UseMapMock --> GetMapData

    GetMapData --> AnalyzeData[LLM 分析综合数据:<br/>- 天气适宜度<br/>- 景点评分<br/>- 交通便利性<br/>- 性价比]

    AnalyzeData --> GenerateRec[生成推荐方案:<br/>1. 目的地列表<br/>2. 推荐理由<br/>3. 行程建议<br/>4. 注意事项]

    GenerateRec --> FormatOutput[格式化输出]

    FormatOutput --> ReturnToUser[返回给用户]

    style GetSearchResults fill:#c8e6c9
    style GetWeatherData fill:#c8e6c9
    style GetMapData fill:#c8e6c9
    style GenerateRec fill:#e1f5ff
```

---

## 6. API 配置优先级流程图

```mermaid
graph LR
    Start[需要 API 配置] --> CheckParam{函数参数<br/>已提供?}

    CheckParam -->|是| UseParam[使用函数参数]
    CheckParam -->|否| CheckEnv{环境变量<br/>已设置?}

    CheckEnv -->|是| UseEnv[使用环境变量]
    CheckEnv -->|否| CheckDefault{有默认值?}

    CheckDefault -->|是| UseDefault[使用默认值]
    CheckDefault -->|否| ThrowError[抛出错误]

    UseParam --> Validate{验证配置}
    UseEnv --> Validate
    UseDefault --> Validate

    Validate -->|有效| Success[配置成功]
    Validate -->|无效| ThrowError

    Success --> Ready[可以使用]

    style UseParam fill:#c8e6c9
    style UseEnv fill:#fff9c4
    style UseDefault fill:#e0e0e0
    style ThrowError fill:#ffcdd2
    style Ready fill:#c8e6c9
```

---

## 7. 多层降级策略总览

```mermaid
graph TB
    Request[用户请求] --> Level1{第一层:<br/>MCP 专项工具}

    Level1 -->|天气| OpenWeather[OpenWeather MCP]
    Level1 -->|地图| BaiduMaps[百度地图 MCP]
    Level1 -->|搜索| DuckDuckGo[DuckDuckGo]

    OpenWeather -->|成功| Return1[返回结果]
    BaiduMaps -->|成功| Return1
    DuckDuckGo -->|成功| Return1

    OpenWeather -->|失败| Level2Weather[第二层:<br/>历史天气数据]
    BaiduMaps -->|失败| Level2Map[第二层:<br/>常见地点数据]
    DuckDuckGo -->|失败| Level2Search[第二层:<br/>浏览器搜索]

    Level2Weather --> Return2[返回降级结果]
    Level2Map --> Return2
    Level2Search -->|成功| Return2
    Level2Search -->|失败| Level3[第三层:<br/>模拟数据]

    Level3 --> Return3[返回模拟结果]

    Return1 --> End[完成]
    Return2 --> End
    Return3 --> End

    style OpenWeather fill:#c8e6c9
    style BaiduMaps fill:#c8e6c9
    style DuckDuckGo fill:#c8e6c9
    style Level2Weather fill:#fff9c4
    style Level2Map fill:#fff9c4
    style Level2Search fill:#ffccbc
    style Level3 fill:#e0e0e0
```

---

## 8. 成本与性能对比流程

```mermaid
graph TB
    subgraph 免费方案
        Free1[DuckDuckGo 搜索<br/>$0/月, 1-2秒]
        Free2[OpenWeather API<br/>$0/月, 100万次]
        Free3[百度地图 API<br/>$0/月, 10万次/天]
        Free4[百度 OneAPI LLM<br/>$0/月, 内部免费]
    end

    subgraph 付费方案
        Paid1[Brave Search<br/>$1/1000次]
        Paid2[Google Custom<br/>$5/1000次]
        Paid3[英伟达 API<br/>$0.2-2/百万tokens]
        Paid4[DeepSeek API<br/>$0.14/百万tokens]
    end

    subgraph 性能对比
        Perf1[MCP 工具<br/>响应时间: 0.5-2秒<br/>稳定性: ⭐⭐⭐⭐⭐]
        Perf2[浏览器兜底<br/>响应时间: 10-30秒<br/>稳定性: ⭐⭐⭐]
        Perf3[模拟数据<br/>响应时间: <0.1秒<br/>稳定性: ⭐⭐⭐⭐⭐]
    end

    Free1 -.推荐.-> Perf1
    Free2 -.推荐.-> Perf1
    Free3 -.推荐.-> Perf1
    Free4 -.推荐.-> Perf1

    Paid1 -.可选.-> Perf1
    Paid3 -.可选.-> Perf1
    Paid4 -.经济.-> Perf1

    style Free1 fill:#c8e6c9
    style Free2 fill:#c8e6c9
    style Free3 fill:#c8e6c9
    style Free4 fill:#c8e6c9
    style Perf1 fill:#e1f5ff
```

---

## 9. 系统初始化流程

```mermaid
sequenceDiagram
    participant User as 用户
    participant System as 系统
    participant LLM as LLM 配置
    participant MCP as MCP 管理器
    participant Tools as 工具层

    User->>System: 启动系统

    System->>LLM: 加载 LLM 配置<br/>(读取 .env)
    LLM-->>System: 返回可用提供商列表

    System->>MCP: 初始化 MCP 管理器
    MCP->>MCP: 读取 mcp_config.yaml
    MCP->>MCP: 启动已启用的 MCP 服务器:<br/>- OpenWeather<br/>- 百度地图
    MCP-->>System: MCP 就绪

    System->>Tools: 初始化工具层
    Tools->>Tools: 检测可用搜索工具:<br/>- DuckDuckGo ✅<br/>- Brave Search ⚠️<br/>- Browser-Use ✅
    Tools-->>System: 工具层就绪

    System->>System: 创建 Agent 实例:<br/>- RecommendAgent<br/>- PlanAgent<br/>- BrowserAgent

    System-->>User: 系统初始化完成<br/>准备接受请求
```

---

## 10. 完整请求处理时序图

```mermaid
sequenceDiagram
    participant User as 用户
    participant Agent as RecommendAgent
    participant Search as 搜索工具
    participant Weather as 天气工具
    participant Map as 地图工具
    participant LLM as LLM API

    User->>Agent: "推荐3月去哪里旅游"

    Agent->>LLM: 解析用户意图
    LLM-->>Agent: {time: "3月", preferences: {}}

    Agent->>Search: 搜索候选目的地<br/>("3月旅游推荐")
    Search->>Search: DuckDuckGo 搜索
    Search-->>Agent: 返回10个候选地

    loop 对每个候选地
        Agent->>Weather: 查询天气<br/>(城市, 3月)
        Weather-->>Agent: 温度、湿度、天气

        Agent->>Map: 查询地点详情<br/>(景点、评分)
        Map-->>Agent: 地点列表、评分
    end

    Agent->>LLM: 综合分析所有数据
    LLM-->>Agent: 推荐方案:<br/>1. 云南昆明<br/>2. 广西桂林<br/>3. 福建厦门

    Agent->>Agent: 格式化输出

    Agent-->>User: 返回详细推荐:<br/>- 目的地列表<br/>- 推荐理由<br/>- 天气情况<br/>- 景点建议<br/>- 行程规划
```

---

## 使用说明

1. **总体架构流程图**：展示整个系统的层次结构
2. **LLM 配置选择流程图**：说明如何选择和配置 LLM
3. **搜索工具智能降级流程图**：展示搜索工具的降级策略
4. **BrowserAgent 执行流程图**：浏览器自动化的详细步骤
5. **旅行推荐 Agent 完整流程图**：推荐功能的完整实现
6. **API 配置优先级流程图**：配置参数的优先级规则
7. **多层降级策略总览**：系统的降级保障机制
8. **成本与性能对比流程**：免费 vs 付费方案对比
9. **系统初始化流程**：系统启动时的初始化过程
10. **完整请求处理时序图**：用户请求的完整处理流程

这些流程图可以使用支持 Mermaid 的工具查看，如：
- GitHub/GitLab（直接渲染）
- VS Code（安装 Mermaid 插件）
- 在线工具：https://mermaid.live/
