# 🗺️ 百度地图 API 集成指南

> **专为国内用户优化** —— 稳定、快速、功能丰富

---

## 📋 目录

1. [为什么选择百度地图](#1-为什么选择百度地图)
2. [获取 API Key](#2-获取-api-key)
3. [配置说明](#3-配置说明)
4. [功能列表](#4-功能列表)
5. [使用示例](#5-使用示例)
6. [常见问题](#6-常见问题)

---

## 1. 为什么选择百度地图

### 🇨🇳 国内优势

| 对比维度 | 百度地图 | Google Maps |
|---------|---------|-------------|
| **国内访问** | ✅ 稳定快速 | ❌ 需要翻墙 |
| **数据准确性** | ✅ 国内数据最全 | ⚠️ 国内数据不全 |
| **API 稳定性** | ✅ 99.9% | ⚠️ 国内不稳定 |
| **中文支持** | ✅ 完美 | ⚠️ 一般 |
| **POI 数量** | ✅ 数千万 | ⚠️ 较少 |
| **价格** | ✅ 免费额度高 | ⚠️ 收费 |

### 🎯 核心功能

- ✅ **地点搜索（POI）**：餐厅、酒店、景点等
- ✅ **路线规划**：驾车、公交、步行、骑行
- ✅ **地址解析**：地址 ↔ 坐标互转
- ✅ **行政区划查询**：省市区数据
- ✅ **地点详情**：电话、营业时间、评分等

---

## 2. 获取 API Key

### 步骤 1：注册账号

访问百度地图开放平台：https://lbsyun.baidu.com/

点击右上角"控制台" → 注册/登录

### 步骤 2：创建应用

1. 进入控制台：https://lbsyun.baidu.com/apiconsole/key
2. 点击"创建应用"
3. 填写应用信息：
   - **应用名称**：AI Travel Agent
   - **应用类型**：服务端
   - **IP 白名单**：0.0.0.0/0（测试用，生产环境建议限制）
4. 提交后获得 **AK（Access Key）**

### 步骤 3：开启服务

在控制台中，确保以下服务已开启：
- ✅ **地点检索服务** - 搜索 POI
- ✅ **路线规划服务** - 规划路线
- ✅ **地理编码服务** - 地址解析
- ✅ **行政区划服务** - 区域查询

---

## 3. 配置说明

### 3.1 配置 API Key

编辑 `.env` 文件：

```env
# 百度地图 API Key
BAIDU_MAPS_API_KEY=你的百度地图AK

# 示例（这不是真实的 Key）
# BAIDU_MAPS_API_KEY=AbCdEf1234567890GhIjKlMnOpQrStUv
```

### 3.2 启用百度地图 MCP

在 `mcp_config.yaml` 中确认配置：

```yaml
servers:
  baidu_maps:
    name: "Baidu Maps MCP Server"
    command: "python"
    args: ["tools/mcp_servers/baidu_maps_server.py"]
    env:
      BAIDU_MAPS_API_KEY: "${BAIDU_MAPS_API_KEY}"
    description: "百度地图服务"
    enabled: true  # 确保为 true
```

### 3.3 测试连接

```bash
# 测试百度地图 MCP Server
python -c "
from mcp_client import MCPManager

with MCPManager() as mcp:
    # 搜索地点
    result = mcp.call_tool('baidu_maps', 'place_search', {
        'query': '餐厅',
        'region': '北京'
    })
    print(result)
"
```

---

## 4. 功能列表

### 4.1 地点搜索（place_search）

**功能**：搜索 POI（餐厅、酒店、景点等）

**参数**：
- `query` (必填): 搜索关键词
- `region` (必填): 城市名称
- `limit` (可选): 返回结果数量，默认 10

**返回**：
```json
{
  "total": 100,
  "places": [
    {
      "name": "北京烤鸭店",
      "address": "北京市东城区前门大街123号",
      "location": {"lat": 39.915, "lng": 116.404},
      "tel": "010-12345678",
      "uid": "abc123"
    }
  ]
}
```

---

### 4.2 路线规划（direction）

**功能**：规划两地之间的路线

**参数**：
- `origin` (必填): 起点（地址或坐标）
- `destination` (必填): 终点（地址或坐标）
- `mode` (可选): 出行方式
  - `driving` - 驾车（默认）
  - `transit` - 公交+地铁
  - `walking` - 步行
  - `riding` - 骑行

**返回**：
```json
{
  "mode": "driving",
  "routes": [
    {
      "distance": 15000,     // 米
      "duration": 1800,      // 秒
      "steps": 12,           // 步骤数
      "toll": 5,             // 过路费（元）
      "traffic_condition": "畅通"
    }
  ]
}
```

---

### 4.3 地址解析（geocoding）

**功能**：将地址转换为坐标

**参数**：
- `address` (必填): 地址
- `city` (可选): 城市名称

**返回**：
```json
{
  "location": {"lat": 39.915, "lng": 116.404},
  "precise": 1,           // 精确度：1=精确，0=大致
  "confidence": 80,       // 可信度（0-100）
  "comprehension": 100,   // 理解度（0-100）
  "level": "门址"         // 地址级别
}
```

---

### 4.4 逆地址解析（reverse_geocoding）

**功能**：将坐标转换为地址

**参数**：
- `lat` (必填): 纬度
- `lng` (必填): 经度

**返回**：
```json
{
  "formatted_address": "北京市东城区前门大街123号",
  "business": "前门,天安门",
  "addressComponent": {
    "province": "北京市",
    "city": "北京市",
    "district": "东城区",
    "street": "前门大街"
  },
  "pois": [...]  // 附近的 POI
}
```

---

### 4.5 地点详情（place_detail）

**功能**：获取地点的详细信息

**参数**：
- `uid` (必填): 地点 UID（从搜索结果获取）
- `scope` (可选): 详细程度，1=基础，2=详细（默认）

**返回**：
```json
{
  "name": "北京烤鸭店",
  "address": "北京市东城区前门大街123号",
  "telephone": "010-12345678",
  "detail_info": {
    "tag": "美食;中餐厅",
    "overall_rating": "4.5",
    "price": "150",
    "shop_hours": "10:00-22:00"
  }
}
```

---

### 4.6 行政区划查询（district）

**功能**：查询行政区划信息

**参数**：
- `keywords` (必填): 区域名称
- `sub_admin` (可选): 是否返回下级行政区，0=否（默认），1=是

**返回**：
```json
{
  "total": 1,
  "districts": [
    {
      "name": "北京市",
      "adcode": "110000",
      "level": "省",
      "center": {"lat": 39.915, "lng": 116.404}
    }
  ]
}
```

---

## 5. 使用示例

### 5.1 在代码中使用

#### 示例 1：搜索附近的餐厅

```python
from mcp_client import MCPManager

with MCPManager() as mcp:
    result = mcp.call_tool("baidu_maps", "place_search", {
        "query": "川菜馆",
        "region": "成都",
        "limit": 5
    })

    for place in result["places"]:
        print(f"- {place['name']}: {place['address']}")
        print(f"  电话: {place['tel']}")
```

#### 示例 2：规划路线

```python
from mcp_client import MCPManager

with MCPManager() as mcp:
    # 驾车路线
    result = mcp.call_tool("baidu_maps", "direction", {
        "origin": "天安门",
        "destination": "故宫",
        "mode": "driving"
    })

    route = result["routes"][0]
    print(f"距离: {route['distance']/1000:.2f} 公里")
    print(f"时间: {route['duration']/60:.0f} 分钟")
    print(f"过路费: {route.get('toll', 0)} 元")
```

#### 示例 3：地址解析

```python
from mcp_client import MCPManager

with MCPManager() as mcp:
    # 地址 → 坐标
    result = mcp.call_tool("baidu_maps", "geocoding", {
        "address": "北京市东城区天安门广场",
        "city": "北京"
    })

    loc = result["location"]
    print(f"坐标: {loc['lat']}, {loc['lng']}")
```

---

### 5.2 在工具中使用

```python
# 使用封装好的工具函数
from tools.utility.map_route_v2 import search_place, get_route_suggestion

# 搜索地点
places = search_place("火锅店", "重庆")
print(places)

# 规划路线
route = get_route_suggestion("解放碑", "洪崖洞", mode="walking")
print(route)
```

---

### 5.3 在 Agent 中使用

```python
from agents.recommend_agent import RecommendAgent

agent = RecommendAgent()

# Agent 会自动调用百度地图工具
result = agent.run_standalone({
    "user_input": "推荐成都附近的景点和路线",
    "destination": "成都",
    "intent": "recommend_destination"
})
```

---

## 6. 常见问题

### Q1: 获取 API Key 失败

**A:** 确保已完成实名认证。访问 https://lbsyun.baidu.com/apiconsole/center 完成认证。

---

### Q2: API 调用返回错误码

**常见错误码：**

| 错误码 | 说明 | 解决方案 |
|--------|------|----------|
| 2 | 参数错误 | 检查参数格式是否正确 |
| 3 | 权限校验失败 | 检查 AK 是否正确 |
| 4 | 配额不足 | 升级配额或等待重置 |
| 5 | AK 不存在 | 重新获取 AK |
| 101 | 服务禁用 | 在控制台开启相应服务 |
| 102 | 配额超限 | 升级配额 |

---

### Q3: 搜索结果为空

**A:**
1. 检查 `region` 参数是否正确
2. 尝试更通用的关键词（如"餐厅"而非"某某餐厅"）
3. 检查城市名称拼写

---

### Q4: 路线规划失败

**A:**
1. 确保起点和终点格式正确
2. 支持的格式：
   - 地址：`"北京市朝阳区"`
   - 坐标：`"39.915,116.404"`
3. 检查 `mode` 参数是否合法

---

### Q5: IP 白名单限制

**A:**
- 测试环境：设置为 `0.0.0.0/0`
- 生产环境：设置为服务器的公网 IP

---

### Q6: 配额不够用怎么办

**免费配额（每天）：**
- 地点检索：30,000 次
- 路线规划：30,000 次
- 地理编码：600,000 次

**如需更多：**
1. 访问控制台升级配额
2. 联系百度地图客服
3. 实施缓存策略减少调用

---

## 7. 最佳实践

### 7.1 缓存策略

```python
from functools import lru_cache

@lru_cache(maxsize=1000)
def search_place_cached(query: str, city: str):
    """缓存搜索结果"""
    return search_place(query, city)
```

### 7.2 错误处理

```python
try:
    result = mcp.call_tool("baidu_maps", "place_search", {...})
except TimeoutError:
    # 超时处理
    result = get_fallback_data()
except ValueError as e:
    # 参数错误
    logger.error(f"参数错误: {e}")
except Exception as e:
    # 其他错误
    logger.error(f"API 调用失败: {e}")
```

### 7.3 降级策略

```python
# 优先使用 MCP，失败时降级到模拟数据
result = search_place("餐厅", "北京", use_mcp=True)

# 如果 MCP 失败，函数会自动返回模拟数据
# 不会影响系统稳定性
```

### 7.4 批量处理

```python
# 批量搜索多个城市
cities = ["北京", "上海", "深圳"]
results = []

for city in cities:
    result = search_place("酒店", city)
    results.append(result)
    time.sleep(0.1)  # 避免触发频率限制
```

---

## 8. API 文档链接

- **百度地图开放平台**: https://lbsyun.baidu.com/
- **控制台**: https://lbsyun.baidu.com/apiconsole/key
- **API 文档**: https://lbsyun.baidu.com/index.php?title=webapi
- **常见问题**: https://lbsyun.baidu.com/index.php?title=lbscloud/faq

---

## 9. 项目文件

| 文件 | 用途 |
|------|------|
| `tools/mcp_servers/baidu_maps_server.py` | 百度地图 MCP Server |
| `tools/utility/map_route_v2.py` | 地图工具封装 |
| `mcp_config.yaml` | MCP 配置（包含百度地图） |
| `.env.example` | 完整环境变量模板（含 LLM + 工具API 说明） |

---

## 🎯 总结

通过集成百度地图 API，项目现在具备：

1. ✅ **国内优化的地图服务**（稳定、快速）
2. ✅ **丰富的 POI 数据**（餐厅、酒店、景点）
3. ✅ **多种路线规划**（驾车、公交、步行、骑行）
4. ✅ **地址解析能力**（地址 ↔ 坐标）
5. ✅ **自动降级策略**（失败时使用模拟数据）

**面试加分项**：
- 展示了对国内技术生态的理解
- 实践了真实的地图 API 集成
- 体现了技术选型能力（百度 vs Google）

---

**🎉 百度地图集成完成！项目现在支持国内最优的地图服务！**
