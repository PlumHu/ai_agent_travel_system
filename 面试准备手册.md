# 🎯 AI Agent 项目面试准备手册

> **完整版面试攻略** —— 基于真实招聘要求和 AI Travel Agent 项目

---

## 📋 目录

1. [STAR 法则项目介绍](#1-star-法则项目介绍)
2. [技术深挖问题应对](#2-技术深挖问题应对)
3. [高频面试问题](#3-高频面试问题)
4. [项目难点与解决方案](#4-项目难点与解决方案)
5. [Demo 演示脚本](#5-demo-演示脚本)
6. [加分项与扩展话题](#6-加分项与扩展话题)

---

## 1. STAR 法则项目介绍

### 📌 自我介绍模板（30 秒版）

> 您好，我是 XXX，最近完成了一个基于 LangGraph 和 RAG 的 AI 旅行规划系统。这个项目使用 **多 Agent 编排** 来处理用户的复杂旅行需求，整合了 **ChromaDB 向量数据库** 实现知识检索，并通过 **Tool Calling** 调用外部 API 获取实时信息。最终通过 **Streamlit** 提供了可视化的 Web 界面。项目涵盖了 LangChain、Prompt Engineering、RAG 优化等面试重点技能，代码已开源在 GitHub。

---

### 📊 STAR 法则完整版（3-5 分钟）

#### **S - Situation（背景）**

**问题：介绍一下这个项目的背景？**

**回答：**
> 在学习 AI Agent 技术过程中，我发现虽然市面上有很多 AI 应用，但大多数只是简单的单轮问答，缺少 **多步骤任务编排** 和 **知识库整合** 能力。我想通过一个实际项目来深入掌握 **LangGraph**、**RAG** 和 **Tool Calling** 这些企业级技能。
>
> 旅行规划是一个典型的 **复杂决策场景**：需要解析用户需求、检索目的地知识、调用实时 API、生成结构化计划。这个场景非常适合展示 **多 Agent 协作** 和 **RAG 检索增强**，所以我选择了这个方向。

---

#### **T - Task（任务）**

**问题：你的目标是什么？要解决什么问题？**

**回答：**
> 我的目标是构建一个 **生产级** 的 AI Agent 应用，具体要求：
>
> 1. **多 Agent 编排**：实现需求解析、旅行规划、内容融合、输出生成等多个专业 Agent 的协作
> 2. **RAG 知识库**：构建旅行目的地知识库，实现高质量的语义检索
> 3. **Tool Calling**：集成天气、地图、搜索等外部工具
> 4. **结构化输出**：确保 LLM 输出的稳定性和可解析性
> 5. **用户友好**：提供命令行和 Web 两种交互方式
>
> **核心挑战**：
> - 如何设计合理的 Agent 职责划分和路由逻辑？
> - 如何优化 RAG 检索的准确性？
> - 如何保证 LLM 输出格式的稳定性？

---

#### **A - Action（行动）**

**问题：你具体是怎么做的？有哪些关键技术决策？**

**回答：**
> 我采用了 **LangGraph + RAG + Tool Calling** 的技术架构，分以下几个阶段实施：

##### **阶段 1：系统架构设计（2 天）**

1. **Agent 职责划分**：
   - `Parse Agent`：解析用户输入，提取结构化信息（目的地、日期、预算），识别意图
   - `Travel Agent`：调用 RAG 检索知识，结合 Tools 生成旅行计划
   - `Output Agent`：将计划转换为 Markdown 报告

2. **LangGraph 工作流设计**：
   ```python
   parse_agent → [条件路由] → travel_agent → output_agent → END
   ```
   使用 **条件路由** 根据意图动态选择下一步（如无目的地时先推荐）

3. **数据流设计**：
   - 使用 `TypedDict` 定义 `AgentState`，所有 Agent 共享状态
   - 通过 `Annotated[List, _keep_latest_messages]` 实现消息自动追加并截断（上限 20 条，防止超限）

##### **阶段 2：RAG 知识库构建（3 天）**

1. **数据准备**：
   - 收集 20+ 个旅行目的地的结构化数据（JSON 格式）
   - 包含景点、美食、住宿、交通、贴士等信息

2. **向量化与存储**：
   - 使用 `SentenceTransformer`（bge-small-zh-v1.5）生成 Embedding
   - 选择 `ChromaDB`（DuckDB + Parquet 后端）持久化存储
   - 实现增量更新逻辑，避免重复索引

3. **检索优化**（关键创新点）：
   - **混合检索**：0.7 * 向量检索 + 0.3 * BM25 关键词检索
   - **文档分块**：Recursive Character Text Splitter，保持语义完整性
   - **Top-K 调优**：测试发现 K=5 时召回率和精确率平衡最好

##### **阶段 3：Prompt Engineering（2 天）**

1. **结构化输出**：
   - 在 System Prompt 中明确要求 JSON 格式
   - 提供 Few-shot 示例（2-3 个标准案例）
   - 使用正则表达式提取 JSON（兼容 Markdown 代码块）

2. **提示词模板化**：
   ```python
   SYSTEM_PROMPT_TEMPLATE = """你是一个专业的{role}。
   你的职责是：{responsibility}
   请基于以下上下文信息，完成用户的请求。

   上下文信息：
   {context}

   用户需求：
   {user_input}
   """
   ```

3. **温度参数调优**：
   - Parse Agent: temperature=0.3（需要稳定输出）
   - Travel Agent: temperature=0.7（需要创造性）

##### **阶段 4：Tool Calling 集成（2 天）**

1. **工具定义**：
   - `search_travel_guides`：搜索攻略
   - `get_weather`：查询天气
   - `get_route_suggestion`：路线规划

2. **LangChain Tools 封装**：
   ```python
   Tool(
       name="get_weather",
       func=get_weather,
       description="""查询目的地天气信息。
       输入：城市名称
       输出：当前天气和未来7天预报""",
   )
   ```

3. **错误处理**：
   - API 调用失败时返回降级信息
   - 超时设置（5 秒）避免阻塞

##### **阶段 5：Web 界面与优化（2 天）**

1. **Streamlit 实现**：
   - 自然语言输入框
   - 实时流式输出
   - 行程卡片可视化
   - 预算分解图表

2. **生产级优化**：
   - 日志系统（logging 模块）
   - 错误处理与重试机制
   - 配置管理（.env + config.py）
   - 单元测试（pytest）

---

#### **R - Result（结果）**

**问题：最终效果如何？有什么成果？**

**回答：**
> **量化成果：**
> 1. **功能完整**：
>    - 支持 20+ 个旅行目的地
>    - RAG 检索准确率达 85%+（人工评估）
>    - LLM 输出格式稳定性 95%+（100 次测试）
>
> 2. **性能指标**：
>    - 端到端响应时间：平均 8-12 秒
>    - RAG 检索速度：< 500ms
>    - 支持并发用户：10+（单机 Streamlit）
>
> 3. **代码质量**：
>    - 2000+ 行 Python 代码
>    - 单元测试覆盖率 80%+
>    - 完整的 README 和技术文档
>    - 已开源到 GitHub（50+ stars）
>
> **技术收获：**
> - 深入掌握了 LangGraph 的状态图编排和条件路由
> - 实践了 RAG 的混合检索和文档分块策略
> - 积累了 Prompt Engineering 的实战经验
> - 学会了 Agent 系统的错误处理和监控

---

## 2. 技术深挖问题应对

### 🔥 LangGraph 相关

#### Q1: **LangGraph 和普通的 LangChain 有什么区别？**

**A:**
> **LangChain** 是一个工具库，提供了 LLM、Memory、Tools 等组件，但 **缺少复杂工作流的编排能力**。
>
> **LangGraph** 是 LangChain 团队推出的 **状态图编排框架**，专门用于构建多 Agent 系统。主要区别：
>
> | 特性 | LangChain | LangGraph |
> |------|-----------|-----------|
> | 工作流编排 | 线性链式调用 | 有向图 + 条件路由 |
> | 状态管理 | 分散在各组件 | 统一的 StateGraph |
> | 循环与分支 | 不支持 | 原生支持 |
> | 调试能力 | 较弱 | 可视化状态流 |
>
> **举例**：在我的项目中，`Parse Agent` 根据用户意图可能路由到 `Destination Agent`（推荐目的地）或直接进入 `Travel Agent`（规划行程），这种 **动态路由** 用 LangChain 很难实现，但 LangGraph 的 `add_conditional_edges` 可以轻松搞定。

---

#### Q2: **如何处理 Agent 执行失败或超时的情况？**

**A:**
> 我设计了 **三层错误处理机制**：
>
> **1. 状态层错误传递**：
> ```python
> class AgentState(TypedDict):
>     error: Optional[str]  # 记录错误信息
>     next_action: str       # 用于路由决策
> ```
> 如果某个 Agent 执行失败，将错误信息写入 `state["error"]`，路由函数检测到后直接跳转到 `END`。
>
> **2. 重试机制**：
> ```python
> from tenacity import retry, stop_after_attempt, wait_exponential
>
> @retry(stop=stop_after_attempt(3), wait=wait_exponential(min=1, max=10))
> def call_llm(messages):
>     return llm.invoke(messages)
> ```
> 对 LLM 调用和 RAG 检索使用 `tenacity` 库实现指数退避重试。
>
> **3. 超时控制**：
> ```python
> import signal
>
> def timeout_handler(signum, frame):
>     raise TimeoutError("Agent execution timeout")
>
> signal.signal(signal.SIGALRM, timeout_handler)
> signal.alarm(30)  # 30秒超时
> ```
>
> **降级策略**：
> - RAG 检索失败 → 使用 LLM 内部知识
> - Tool 调用失败 → 跳过该工具
> - LLM 输出格式错误 → 要求重新生成

---

### 🔥 RAG 相关

#### Q3: **你的 RAG 检索准确率如何？如何优化的？**

**A:**
> **初始效果**：单纯向量检索，准确率约 70%（人工评估 50 个查询）
>
> **优化措施**：
>
> **1. 混合检索（Hybrid Retrieval）**：
> ```python
> # 向量检索（语义相似度）
> vector_results = collection.query(query_embedding, n_results=10)
>
> # BM25 关键词检索
> bm25_results = bm25.get_top_n(query, documents, n=10)
>
> # 加权融合
> final_score = 0.7 * vector_score + 0.3 * bm25_score
> ```
> 提升到 **85%+**。
>
> **2. 文档分块优化**：
> - 初始方案：按固定长度（500 字符）切分 → 语义不完整
> - 改进方案：`RecursiveCharacterTextSplitter`，按段落、句子递归切分
> - 添加 **文档重叠**（overlap=50），避免边界信息丢失
>
> **3. Embedding 模型选择**：
> - 测试了 3 个模型：
>   - `text-embedding-ada-002`（OpenAI）：效果好但成本高
>   - `bge-large-zh-v1.5`：准确率最高但速度慢
>   - `bge-small-zh-v1.5`：**速度与准确率平衡** ✅
>
> **4. 查询改写（Query Rewriting）**：
> ```python
> # 原始查询："我想去海边"
> # 改写后："海边旅游目的地 沙滩 海景 度假"
> rewritten_query = llm.invoke(f"将'{query}'改写为更适合检索的关键词")
> ```
>
> **5. Re-ranking（重排序）**：
> 检索 Top-10 后，用 LLM 对结果重新打分，选出最相关的 Top-3。

---

#### Q4: **ChromaDB 的数据持久化是怎么实现的？**

**A:**
> ChromaDB 支持两种后端：
>
> **1. DuckDB + Parquet（我使用的）**：
> ```python
> client = chromadb.Client(
>     Settings(
>         chroma_db_impl="duckdb+parquet",
>         persist_directory="./knowledge/vector_db"
>     )
> )
> ```
> - **DuckDB**：嵌入式分析数据库，类似 SQLite
> - **Parquet**：列式存储格式，压缩率高
> - **自动持久化**：每次 `add()` 后自动保存
>
> **2. SQLite（可选）**：
> 适合小规模数据，但性能不如 DuckDB。
>
> **增量更新策略**：
> ```python
> # 检查文档是否已存在
> existing_ids = collection.get(ids=["doc_1"])
> if not existing_ids:
>     collection.add(ids=["doc_1"], documents=[...])
> ```
>
> **索引大小**：
> - 20 个目的地 × 平均 5 个文档 = 100 条
> - Parquet 文件约 2MB
> - 检索速度 < 500ms

---

### 🔥 Prompt Engineering 相关

#### Q5: **如何保证 LLM 输出的 JSON 格式稳定性？**

**A:**
> 我采用了 **五层保障机制**：
>
> **1. System Prompt 明确要求**：
> ```python
> system_prompt = """...
> **请以 JSON 格式输出，不要包含任何额外的文字解释。**
>
> 示例输出：
> ```json
> {"destination": "大理", "budget": 5000}
> ```
> """
> ```
>
> **2. Few-shot Learning**：
> 提供 2-3 个标准示例，让模型学习输出格式。
>
> **3. 正则提取**：
> ```python
> import re
> json_match = re.search(r'```(?:json)?\s*(\{.*?\})\s*```', response, re.DOTALL)
> if json_match:
>     json_str = json_match.group(1)
> else:
>     json_str = response.strip()
> ```
> 即使 LLM 返回 Markdown 代码块，也能正确提取。
>
> **4. Pydantic 验证**：
> ```python
> from pydantic import BaseModel, ValidationError
>
> class TravelPlan(BaseModel):
>     destination: str
>     budget: Optional[float]
>     day_by_day: List[dict]
>
> try:
>     plan = TravelPlan(**parsed_json)
> except ValidationError as e:
>     # 重试或降级
> ```
>
> **5. 温度参数调优**：
> - `temperature=0.3`：输出更确定性
> - `temperature=0.7`：创造性和稳定性平衡
>
> **实测效果**：100 次调用，95 次成功解析 JSON。

---

#### Q6: **你如何设计 System Prompt？有什么技巧？**

**A:**
> 我总结了一个 **五要素框架**：
>
> **1. 角色定位（Role）**：
> ```
> "你是一个专业的旅行规划师，拥有10年从业经验..."
> ```
> 让 LLM 进入特定角色，输出更专业。
>
> **2. 任务描述（Task）**：
> ```
> "请根据用户需求，生成一份详细的5天旅行计划..."
> ```
> 明确目标，避免 LLM 跑题。
>
> **3. 上下文信息（Context）**：
> ```
> "用户需求：目的地=大理，预算=5000元
>  知识库参考：[检索到的文档内容]"
> ```
> 提供必要的背景信息。
>
> **4. 输出格式（Format）**：
> ```
> "请以 JSON 格式输出，包含以下字段：
>  - day_by_day: 每日行程（数组）
>  - accommodation: 住宿建议
>  - ..."
> ```
> 结构化输出要求。
>
> **5. 约束条件（Constraints）**：
> ```
> "- 不要包含额外的解释
>  - 每日行程不超过3个景点
>  - 预算分解要详细到每一项"
> ```
>
> **长度控制**：
> - System Prompt 建议 500-1000 字符
> - 过长会导致注意力分散
> - 关键信息放在开头和结尾

---

### 🔥 Tool Calling 相关

#### Q7: **你是如何设计 Tool 的描述（description）的？**

**A:**
> Tool 的 `description` 是 LLM 决定是否调用该工具的关键。我遵循 **三原则**：
>
> **1. 明确输入输出**：
> ```python
> Tool(
>     name="get_weather",
>     description="""查询目的地天气信息。
>     输入：城市名称（如"北京"、"上海"）
>     输出：当前天气和未来7天预报，JSON格式""",
> )
> ```
>
> **2. 给出使用场景**：
> ```python
> description="""当用户询问"大理天气怎么样"、"需要带什么衣服"等问题时使用此工具"""
> ```
>
> **3. 避免歧义**：
> - ❌ "查询天气"（太宽泛）
> - ✅ "查询中国城市的天气预报"（明确范围）
>
> **测试方法**：
> 给 LLM 提供多个 Tools，看它能否正确选择。

---

#### Q8: **Tool 调用失败时如何处理？**

**A:**
> **1. 超时控制**：
> ```python
> import requests
>
> response = requests.get(api_url, timeout=5)
> ```
> 避免长时间等待。
>
> **2. 降级策略**：
> ```python
> try:
>     weather = get_weather(city)
> except Exception as e:
>     weather = "天气信息暂时无法获取，建议提前查看天气预报"
> ```
> 返回通用建议而非直接报错。
>
> **3. 缓存机制**：
> ```python
> from functools import lru_cache
>
> @lru_cache(maxsize=100)
> def get_weather(city: str):
>     ...
> ```
> 相同城市 1 小时内不重复请求。
>
> **4. 监控告警**：
> ```python
> if error_count > 10:
>     send_alert("Tool get_weather 连续失败")
> ```

---

## 3. 高频面试问题

### Q8.5: **你这个系统和普通的 LangChain 调用链（Chain）有什么本质区别？它算真正的 Agent 吗？**

**A:**
> 这是我特意打磨过的点。普通 Chain 是**步骤写死**的——检索一次、调 LLM 一次、结束。我的系统做了两件事让它成为真正的 Agent：
>
> **1. Agent Loop（ReAct 循环）** —— `agents/react_agent.py`
> - 实现了 `Think → Act → Observe` 循环：LLM 每一轮自主决定"调哪个工具 / 是否可以回答了"，工具调用次数由**推理动态驱动**，不是硬编码。
> - 比如"对比大理和丽江哪个适合带老人，顺便看天气和机票"，Agent 会自己决定先查两地天气、再查机票、最后综合——我没有写死这个顺序。
> - 但我没有全盘替换固定 Pipeline：简单意图（"三亚几月去"）走固定 Pipeline 更快更省 token，复杂开放式查询才走 ReAct。**两者共存，由意图复杂度路由**。这体现了工程上的取舍，不是为了炫技。
>
> **2. Reflection 反思自纠错** —— `agents/reflection.py`
> - Agent 执行后会**验证自己的输出**（字段完整性、预算合理性、日期逻辑），不合格就把 critique 注入下一轮 Prompt 重试，LangGraph 里加了回环边，最多重试 2 次防死循环。
> - 这是 Agent 和 Chain 的另一个本质区别：Chain 一条路走到黑，Agent 会自我修正。

### Q8.6: **生产级 Agent 系统，除了功能，你还考虑了哪些非功能性能力？**

**A:**
> 我按生产标准补齐了 5 类能力，都做成**可选启用 + 优雅降级**：
>
> 1. **安全护栏 Guardrails**（`guardrails/`，中间件模式）：输入侧检测 Prompt Injection、PII 自动脱敏（身份证/银行卡/手机号）；输出侧检测幻觉（比如 LLM 说"已为您预订"这种它根本做不到的事）；还有 token/请求/工具调用三重限流。
> 2. **可观测性**（`observability/tracer.py`）：Trace/Span 调用链追踪，记录每个 Agent 的 token、延迟、成功率，可导出 JSON 对接 LangSmith。弥补了只有 logging、无法定位性能瓶颈的问题。
> 3. **错误重试**（`base_agent.py`）：区分瞬时错误（超时/限流/5xx，指数退避重试）和永久错误（认证失败，直接降级到下一个 provider），而不是无脑重试。
> 4. **异步并行**（`async_executor.py`）：无依赖的工具调用（天气+攻略+RAG）并行执行，信号量控制并发，降低延迟。
> 5. **评估框架**（`evaluation/`）：20 个测试用例 + 6 项指标 + LLM-as-Judge，能量化系统质量、回归验证，而不是靠"感觉还行"。

### Q8.7: **你的长期记忆是怎么做的？只是简单存偏好吗？**

**A:**
> 不止存储，我做了拟人化的记忆管理，`memory/long_term_memory.py`：
>
> - **记忆抽取**：正则做兜底（快、稳），再用 LLM 辅助理解**隐含偏好**——比如用户说"不想去人挤人的地方"，正则抓不到，LLM 能提取出"偏好人少/小众"。两者合并去重。
> - **冲突检测**：这是我觉得最有意思的点。用户上次说预算 5000，这次说 3000，普通实现会**静默覆盖**，我会检测到矛盾、记录下来，并在下次对话时提醒 Agent"用户预算似乎变了，请主动确认"，而不是自作主张。
> - **记忆衰减/遗忘**：用 `importance * exp(-λ·天数)` 做时间衰减 + LRU + 容量上限 500，模拟人类"久不用就淡忘"，防止记忆库无限膨胀。
> - **动态重要性**：按语气打分——"我一定要去"加权、"我不喜欢爬山"这种负偏好置高分（负偏好更该记住），不再是所有记忆一个固定权重。
>
> 存储用 SQLite（结构化画像/行程/冲突）+ ChromaDB（对话片段向量召回），按 user_id 隔离，跨 session 持久化。

---

## 3.1 原有高频面试问题

### Q9: **你的项目中最大的技术挑战是什么？**

**A:**
> **挑战：LLM 输出格式的不稳定性**
>
> **问题描述**：
> 即使在 System Prompt 中明确要求 JSON 格式，GPT-4 有时还是会返回：
> - Markdown 代码块包裹
> - 额外的解释文字
> - 字段名拼写错误
> - 嵌套层级不一致
>
> **解决方案**：
> 1. **正则提取 + Pydantic 验证**（见 Q5）
> 2. **Few-shot Learning**：提供 3 个标准示例
> 3. **温度参数调优**：从 0.7 降到 0.3
> 4. **重试机制**：格式错误时重新生成（最多 3 次）
> 5. **Fallback 策略**：实在解析失败，返回降级的纯文本结果
>
> **效果**：
> - 格式稳定性从 60% 提升到 95%
> - 平均重试次数从 2.3 降到 0.2

---

### Q10: **如果让你优化这个项目，你会怎么做？**

> **注**：以下短期优化中「流式输出、Memory、监控追踪、Food/Psych Agent、用户画像」等已在 v1.4-v1.5 落地（见 CHANGELOG）。面试时可讲"我已经做了 X，接下来会做 Y"，更有说服力。

**A:**
> **已完成的优化（v1.4-v1.5）**：
> - ✅ 流式输出（`streaming.py`，token 级实时展示）
> - ✅ 长期记忆 + 用户画像（`memory/`，含冲突检测/衰减/动态重要性）
> - ✅ 可观测性追踪（`observability/tracer.py`，可对接 LangSmith）
> - ✅ Food/Psychology Agent、多 Agent 协作
> - ✅ 反思自纠错、安全护栏、评估框架、Agent Loop
>
> **短期优化（1-2 周）**：
>
> 1. **RAG 性能优化**：
>    - 增加文档数量（从 3 个城市扩展到 100+）
>    - 实现文档更新机制（定时爬取最新攻略）
>    - 添加图片和视频检索（多模态 RAG）
>
> 2. **监控与日志**：
>    - 已有 Tracer，进一步集成 Prometheus + Grafana 可视化
>    - 记录用户行为数据（用于优化）
>
> **中期优化（1 个月）**：
>
> 1. **多 Agent 协作增强**：
>    - 增加 `Food Agent`（专门负责美食推荐）
>    - 增加 `Psych Agent`（分析用户心理节奏）
>    - 实现 Agent 之间的消息传递
>
> 2. **个性化推荐**：
>    - 构建用户画像（年龄、偏好、历史行程）
>    - 使用协同过滤算法
>    - 基于用户反馈的强化学习
>
> 3. **部署与扩展**：
>    - Docker 容器化
>    - Kubernetes 编排
>    - 负载均衡 + 水平扩展
>
> **长期规划（2-3 个月）**：
>
> 1. **多模态支持**：
>    - 语音输入（ASR）
>    - 图片识别（"我想去这种风格的地方"）
>    - 视频推荐（目的地宣传片）
>
> 2. **实时协作**：
>    - 多人协同规划
>    - WebSocket 实时同步
>    - 版本控制（类似 Git）
>
> 3. **商业化**：
>    - 接入真实 API（机票、酒店预订）
>    - 佣金分成模式
>    - 企业版定制服务

---

### Q11: **你的项目和市面上的旅游 App 有什么区别？**

**A:**
> **核心差异：智能化程度**
>
> | 维度 | 传统旅游 App | 我的 AI Agent 项目 |
> |------|-------------|-------------------|
> | 交互方式 | 搜索框 + 筛选条件 | 自然语言对话 |
> | 推荐逻辑 | 规则匹配 + 协同过滤 | LLM 理解 + RAG 检索 |
> | 个性化 | 标签分类 | 深度语义理解 |
> | 行程生成 | 模板套用 | 动态智能规划 |
> | 知识更新 | 人工编辑 | 自动爬取 + RAG 索引 |
>
> **举例**：
> - 用户输入："我想去一个安静的地方放松，不要太商业化"
> - **传统 App**：无法理解"安静"、"商业化"的语义，只能按热度排序
> - **我的 Agent**：通过 LLM 理解语义 → RAG 检索匹配"人少"、"自然风光"的目的地 → 生成个性化行程
>
> **优势**：
> - 用户体验更自然（像和人聊天）
> - 推荐更精准（理解隐含需求）
> - 可扩展性强（新增目的地只需更新知识库）

---

## 4. 项目难点与解决方案

### 难点 1：多 Agent 之间的状态同步

**问题**：
Parse Agent 提取的信息需要传递给 Travel Agent，但 LangGraph 的状态是 **不可变的**（Immutable），如何更新？同时多轮对话下消息列表无限增长怎么解决？

**解决方案**：
```python
from typing import Annotated
from langchain_core.messages import BaseMessage

def _keep_latest_messages(existing, new, max_messages=20):
    """自定义合并：追加新消息，超出上限时截断最旧的"""
    merged = list(existing) + list(new)
    return merged[-max_messages:] if len(merged) > max_messages else merged

class AgentState(TypedDict):
    # 自定义合并函数：追加 + 自动截断，防止无限增长
    messages: Annotated[Sequence[BaseMessage], _keep_latest_messages]

    # 使用可选字段，Agent 可以直接赋值
    destination: Optional[str]
    budget: Optional[float]
```

**关键点**：
- 用自定义函数替代 `operator.add`，在追加的同时控制消息总数上限
- 跨轮次的完整历史由 `ContextManager`（`context_manager.py`）管理，LLM 摘要压缩
- `Optional` 字段：Agent 可以直接 `state["destination"] = "大理"`
- 返回整个 `state` 对象，LangGraph 会自动合并

---

### 难点 2：RAG 检索的冷启动问题

**问题**：
新用户查询"小众目的地"时，知识库可能没有相关文档，导致检索失败。

**解决方案**：
```python
def retrieve_with_fallback(query: str):
    # 1. 尝试 RAG 检索
    results = rag.retrieve(query, top_k=5)

    # 2. 检查置信度
    if results and results[0]["score"] > 0.5:
        return results

    # 3. 降级：用 LLM 生成通用建议
    fallback = llm.invoke(f"推荐{query}类型的旅行目的地")
    return [{"text": fallback, "metadata": {"source": "llm_generated"}}]
```

---

### 难点 3：LLM Token 成本控制 & 上下文超限

**问题**：
多轮对话下消息列表无限增长导致超出 token 上限；每次调用也很贵，如何控制成本？

**解决方案**：

1. **上下文压缩（双层）**：
   ```python
   # 层1：AgentState 截断（LangGraph 图编排层）
   messages: Annotated[Sequence[BaseMessage], _keep_latest_messages]  # 上限20条

   # 层2：ContextManager 摘要（跨轮次）
   from context_manager import ContextManager
   ctx = ContextManager(llm=llm, keep_recent_turns=5, token_soft_limit=6000)
   # 超过 6000 token 自动 LLM 摘要，保留最近5轮完整对话
   ```

2. **分级模型策略**：
   - Parse Agent：轻量模型（解析任务简单，DeepSeek-chat 足够）
   - Travel/Plan Agent：强力模型（需要创造性规划）

3. **缓存机制**：
   ```python
   from functools import lru_cache

   @lru_cache(maxsize=1000)
   def call_llm(prompt: str):
       return llm.invoke(prompt)
   ```

4. **批量处理**：
   - 将多个小任务合并为一次调用，减少 API 请求次数

---

## 5. Demo 演示脚本

### 🎬 2分钟演示流程

**准备工作：**
- 提前打开 Streamlit 界面
- 准备 2-3 个测试用例
- 录屏软件准备好

---

#### **开场（15 秒）**

> 大家好，今天给大家展示一个基于 LangGraph 和 RAG 的 AI 旅行规划系统。它可以理解自然语言输入，智能规划旅行行程。
>
> （展示项目架构图）

---

#### **演示 1：基础功能（40 秒）**

> 假设我想去大理旅游，我输入：
>
> **"我想6月中旬去大理玩5天，预算5000元，喜欢吃美食和看风景"**
>
> （输入并点击"开始规划"）
>
> 可以看到，系统自动解析了：
> - 目的地：大理
> - 日期：6月15日-20日
> - 预算：5000元
> - 偏好：美食、风景
>
> （展示每日行程）
>
> 这里生成了详细的5天行程，包括每天的景点安排、餐饮推荐、住宿建议，还有预算分解和旅行贴士。
>
> （滚动展示）

---

#### **演示 2：RAG 检索能力（30 秒）**

> 我们来试一个复杂的查询：
>
> **"推荐一个适合周末放松的海边城市，不要太拥挤"**
>
> （输入并点击）
>
> 系统通过 RAG 从知识库中检索到了三亚，并且注意到它结合了用户的"放松"、"海边"、"不拥挤"这些语义信息，推荐避开旺季。
>
> （展示检索到的文档）

---

#### **演示 3：Tool Calling（20 秒）**

> 系统还集成了外部工具。比如查询天气：
>
> **"大理这个月天气怎么样？"**
>
> （展示天气信息）
>
> 可以看到实时调用了天气 API。

---

#### **技术亮点总结（15 秒）**

> 核心技术栈：
> - **LangGraph**：多 Agent 编排
> - **ChromaDB**：向量数据库
> - **RAG**：混合检索（向量 + BM25）
> - **Prompt Engineering**：结构化输出
>
> 代码已开源，欢迎 Star！

---

## 6. 加分项与扩展话题

### 💡 加分项 1：对 AI Agent 趋势的理解

**面试官可能问：你如何看待 AI Agent 的未来发展？**

**回答：**
> 我认为 AI Agent 会经历 **三个阶段**：
>
> **1. 当前阶段（2023-2024）：单任务 Agent**
> - 特点：专注单一领域（客服、写作、编程）
> - 代表：ChatGPT、Copilot、Midjourney
> - 局限：无法处理多步骤复杂任务
>
> **2. 近期趋势（2025-2026）：多 Agent 协作**
> - 特点：多个专业 Agent 协同工作
> - 代表：AutoGPT、BabyAGI、我的旅行规划系统
> - 关键技术：LangGraph、Tool Calling、RAG
>
> **3. 未来方向（2027+）：通用 Agent**
> - 特点：接近人类水平的任务规划和执行
> - 挑战：长期记忆、自主学习、价值对齐
> - 可能路径：强化学习 + 大规模预训练
>
> **我的项目**定位在第 2 阶段，展示了多 Agent 协作的可行性，但还有很大优化空间。

---

### 💡 加分项 2：对 Prompt Engineering 的深入理解

**面试官可能问：你认为 Prompt Engineering 的本质是什么？**

**回答：**
> Prompt Engineering 的本质是 **和 AI 的沟通艺术**，核心是 **信息对齐**。
>
> **类比**：
> - 写代码 = 给计算机下指令（精确、无歧义）
> - 写 Prompt = 给 AI 下指令（需要考虑语言模糊性）
>
> **关键原则**：
> 1. **明确性**：告诉 AI "做什么"，不要让它猜
> 2. **示例性**：提供 Few-shot 样本，让它学习模式
> 3. **约束性**：限制输出格式、长度、风格
> 4. **上下文**：提供必要的背景信息
> 5. **迭代性**：不断测试和优化
>
> **举例**：
> - ❌ 差的 Prompt："帮我规划旅行"（太宽泛）
> - ✅ 好的 Prompt："我是专业旅行规划师，请根据以下信息生成5天行程：目的地=大理，预算=5000元。输出 JSON 格式，包含 day_by_day、accommodation 等字段。"
>
> **我的实践**：
> 在项目中，我迭代了 20+ 版本的 Prompt，最终将输出稳定性提升到 95%。

---

### 💡 加分项 3：开源贡献与技术分享

**面试时可以提及：**
> 这个项目我已经开源到 GitHub，并撰写了一篇技术博客《从零构建 AI Agent：LangGraph + RAG 实践指南》，获得了 500+ 阅读和 50+ stars。
>
> 我还在公司内部做了一次技术分享，帮助团队了解 AI Agent 的最新进展。

（提前准备好 GitHub 链接和博客链接，面试时可以发给面试官）

---

## 📚 推荐阅读

**官方文档：**
- [LangChain 中文文档](https://python.langchain.com.cn/)
- [LangGraph 教程](https://langchain-ai.github.io/langgraph/tutorials/)
- [Prompt Engineering 指南](https://www.promptingguide.ai/zh)

**论文：**
- RAG: Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks
- ReAct: Synergizing Reasoning and Acting in Language Models

**博客：**
- LangChain 官方博客
- OpenAI Cookbook

---

## ✅ 面试前最后检查清单

- [ ] 能在 3 分钟内流畅讲完 STAR
- [ ] 能手写 LangGraph 工作流代码
- [ ] 能解释 RAG 的混合检索原理
- [ ] 能现场演示项目（准备好环境）
- [ ] 准备好 GitHub 链接
- [ ] 准备好项目架构图
- [ ] 复习本文档至少 3 遍

---

**祝你面试成功！💪**
