# AI Agent 旅行规划系统 — 系统设计文档

> 版本：1.5 | 日期：2026-07-08

---

## 1. 系统概述

AI Agent 旅行规划系统是一个多 Agent 协同的智能旅行助手，能够根据用户需求自动完成目的地推荐、行程规划、美食建议、健康评估并生成完整旅行报告。

**核心能力：**
- 自然语言理解用户旅行需求
- 多 Agent 流水线协同处理
- RAG 知识库 + 混合检索
- 多 LLM 提供商支持（OpenAI 兼容接口）
- 三级工具降级策略（API → 免费搜索 → 浏览器自动化）
- 多轮对话上下文压缩（滑动窗口 + LLM 摘要，防止 token 超限）
- 药物-食物相互作用检测（内置，无需 API）

---

## 2. 架构总览

```
用户输入
   │
   ▼
┌─────────────────────────────────────────────┐
│         输入安全护栏（InputGuard）            │  ← 注入检测/PII脱敏/限流
└───────────────┬─────────────────────────────┘
                │
   ┌────────────┴────────────┐
   ▼                         ▼
┌──────────────┐   ┌───────────────────────────┐
│ 固定 Pipeline │   │  Agent Loop（ReActAgent）  │
│ 简单意图/快速 │   │  复杂查询/Think-Act-Observe│
│ run_pipeline  │   │  run_react                 │
└───────┬──────┘   └──────────┬────────────────┘
        │                     │
        ▼                     │
┌─────────────────────────────────────────────┐
│         Agent 编排层（13个Agent）             │
│  ParseAgent → RecommendAgent → PlanAgent     │
│    → FoodAgent → PsychologyAgent             │
│    → MergeAgent → OutputAgent                │
│  ReActAgent（循环）| ReflectionMixin（自纠错）│
│      (BrowserAgent 兜底)                     │
└───────────┬────────────────┬────────────────┘
            │                │
            ▼                ▼
┌───────────────┐   ┌────────────────────────┐
│  RAG 知识库   │   │       工具层            │
│  ChromaDB     │   │  天气 / 地图 / 机票     │
│  HybridSearch │   │  酒店 / 搜索 / 过敏原   │
└───────────────┘   └──────────┬─────────────┘
            │                  │
            ▼                  ▼
┌───────────────┐   ┌──────────────────┐
│  三层 Memory  │   │   MCP 协议层     │
│ 短期/长期/语义 │   │  OpenWeather     │
│ (冲突/衰减/    │   │  百度地图        │
│  动态重要性)   │   │  Brave Search    │
└───────────────┘   └──────────────────┘
                │
                ▼
┌─────────────────────────────────────────────┐
│        输出安全护栏（OutputGuard）            │  ← 幻觉检测/置信度标记
└─────────────────────────────────────────────┘

横切能力：Reflection 反思 | Guardrails 护栏 | Tracer 可观测性
         | AsyncExecutor 异步并行 | StreamingCallback 流式 | Planner 动态规划
```

---

## 3. 模块设计

### 3.1 入口层

#### AgentManager（`agent_manager.py`）

轻量灵活模式，供 Web UI 使用。集成安全护栏、流式回调、HITL 确认、可观测性追踪。

- `register_agent(name, agent)` — 注册 Agent
- `run_agent(name, input_data)` — 独立调用单个 Agent（自动记 Trace span）
- `run_pipeline(user_input)` — 自动路由，执行完整流水线（含护栏 + HITL）
- `run_react(user_input)` — 复杂开放式查询走 ReAct 循环（Think-Act-Observe）
- `get_trace_summary()` / `export_traces()` — 可观测性
- `get_memory_conflicts()` / `decay_memories()` — 长期记忆管理

#### LangGraph 模式（`main.py`）

图编排模式，标准化状态流转：

```
parse_agent → [条件路由] → travel_agent → output_agent
```

路由逻辑依据 `AgentState.intent` 字段决定下一节点。

---

### 3.2 Agent 层

#### 基类设计（`agents/base_agent.py`）— 模板方法模式

```
BaseAgent（抽象, 混入 ReflectionMixin）
  ├── execute(state) → AgentState       # 子类实现
  ├── run_standalone(input_data)        # 独立运行入口
  ├── _invoke_with_fallback(...)        # LLM 调用（多provider兜底 + 瞬时错误退避重试 + 流式回调）
  ├── execute_with_reflection(...)      # 反思循环（来自 ReflectionMixin）
  ├── _prepare_state(input_data)        # Hook：状态准备
  ├── _extract_output(state)            # Hook：输出提取
  └── validate_input(input_data)        # Hook：输入验证
```

每个 Agent 既可作为 LangGraph 节点使用，也可独立调用。所有 Agent 通过混入 `ReflectionMixin` 获得自纠错能力，通过 `streaming_callback` 支持流式输出。

#### Agent 职责表

| Agent | 文件 | 输入 | 输出 | 长期记忆 |
|-------|------|------|------|---------|
| ParseAgent | `parse_agent_v2.py` | 用户自然语言 | 结构化意图 + 参数 | ✅ 注入用户画像辅助补全缺省字段 |
| RecommendAgent | `recommend_agent.py` | 需求 / 目的地 | 目的地推荐 / 最佳时间 | ✅ system_prompt 内嵌历史行程和偏好 |
| PlanAgent | `plan_agent.py` | 目的地、日期、预算 | 逐日行程 JSON | — |
| FoodAgent | `food_agent.py` | 目的地、健康信息 | 美食推荐 + 过敏原报告 | — |
| PsychologyAgent | `psychology_agent.py` | 行程强度 | 压力评估 + 节奏建议 | — |
| MergeAgent | `merge_agent.py` | 多 Agent 中间结果 | 融合内容 | — |
| OutputAgent | `output_agent.py` | 融合内容 | Markdown 报告 + 自媒体 JSON | — |
| BrowserAgent | `browser_agent.py` | 搜索查询 | 网页抓取结果（兜底） | — |
| **ReActAgent** | `react_agent.py` | 复杂开放式查询 | 循环调用工具后的综合答案 | — |

#### 反思自纠错（`agents/reflection.py`）

- `ReflectionMixin.execute_with_reflection(state, execute_fn, validate_fn)`：执行 → 验证 → 失败注入 critique 重试的闭环
- 验证器：`ParseOutputValidator`（字段/格式/日期逻辑，规则式零成本）、`TravelPlanValidator`（完整度/预算合理性）、`RecommendationValidator`、`LLMJudgeValidator`（LLM 综合评分）
- LangGraph 回环边（`main.py`）：`route_after_parse` / `route_after_travel` 检测 `_reflection_critique` 存在且未超 `MAX_REFLECTION_RETRIES(=2)` 时回退重试

#### Agent Loop（`agents/react_agent.py`）

- Think→Act→Observe 循环，`ToolSpec` 包装现有工具供 LLM 动态调度
- 复用 `BudgetLimiter` 限制工具调用次数，工具异常隔离为 observation，`max_iterations` 强制终止
- 与固定 Pipeline 共存，由意图复杂度路由

---

### 3.3 状态管理（`state.py`）

使用 `TypedDict` 定义 `AgentState`，`messages` 字段使用自定义合并函数 `_keep_latest_messages` 替代原来的 `operator.add`，超过 20 条消息时自动截断最旧的消息，防止 LangGraph 图编排下消息列表无限增长。

**关键字段：**

```python
AgentState
  ├── messages: Annotated[Sequence[BaseMessage], _keep_latest_messages]  # 自动截断，最多保留20条
  ├── user_input: str
  ├── intent: str              # plan_trip / recommend_destination / recommend_time / ...
  ├── destination: Optional[str]
  ├── start_date / end_date: Optional[str]
  ├── budget: Optional[float]
  ├── health_info: Dict        # 饮食限制、过敏原等
  ├── retrieved_docs: List[Dict]   # RAG 检索结果
  ├── travel_plan / food_advice / psychology_report  # Agent 输出
  ├── merged_content: str      # MergeAgent 融合结果
  ├── client_report: str       # 最终 Markdown 报告
  ├── reflection_attempts / _reflection_critique      # 反思机制
  ├── guardrail_warnings       # 安全护栏警告
  ├── react_scratchpad / react_iterations / react_answer  # ReAct 循环
  └── current_step / error / next_action
```

---

### 3.3.1 上下文压缩（`context_manager.py`）

多轮对话下，消息历史持续增长会导致 token 超限。系统通过两层机制解决：

**层 1 — AgentState 滑动截断**：`_keep_latest_messages` 合并函数，LangGraph 图节点每次追加消息后保留最新 20 条。

**层 2 — ContextManager 摘要压缩**：`AgentManager` 持有 `ContextManager` 实例，跨轮次管理完整对话历史。

```
ContextManager 工作流程：

每轮对话
   │
   ▼
add_turn(user_msg, assistant_msg)
   │
   ├─ 消息数 <= keep_recent_turns × 2 → 直接追加
   │
   └─ 估算 token > token_soft_limit（6000）→ 触发压缩
              │
              ├─ 有 LLM → LLM 摘要压缩（200字以内）
              └─ 无 LLM → 规则提取（目的地/时间/预算关键词）
                             │
                             ▼
                    _summary 更新（追加）
                    _history 重置为最近 N 轮

get_messages() 返回：
   [system_prompt] + [历史摘要（如有）] + [最近 N 轮完整对话]
```

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `keep_recent_turns` | 5 | 保留最近几轮完整对话 |
| `token_soft_limit` | 6000 | 触发压缩的 token 估算上限 |
| `max_messages`（AgentState） | 20 | LangGraph 消息条数上限 |

**持久化接口：**

```python
state = manager.save_context()   # 导出
manager.load_context(state)       # 跨进程恢复
manager.reset_context()           # 开始新会话
```

---

### 3.4 RAG 知识库（`knowledge/`）

#### 混合检索器（`knowledge/retriever.py`）

```
HybridRetriever
  ├── vector_weight = 0.6    # 向量检索权重
  ├── bm25_weight   = 0.4    # BM25 检索权重
  └── top_k = 5
```

**检索流程：**

```
query
  ├──→ 向量检索（ChromaDB + BAAI/bge-small-zh-v1.5）→ 语义相似候选集
  └──→ BM25 检索（关键词匹配）→ 精确匹配候选集
              ↓
        RRF 融合（k=60）
              ↓
         加权排序，取 top_k
```

RRF 分数公式：
```
score = vector_weight × 1/(k + vector_rank) + bm25_weight × 1/(k + bm25_rank)
```

#### 知识库内容

- `knowledge/raw_data/destinations/` — 目的地 JSON（大理、丽江、三亚等）
- `knowledge/raw_data/customs/` — 当地风俗禁忌
- `knowledge/raw_data/features/` — 特色体验
- `knowledge/vector_db/` — ChromaDB 持久化目录

#### 三层记忆系统（已实现）

| 记忆类型 | 实现文件 | 存储 | 内容 |
|---------|---------|------|------|
| 短期记忆 | `context_manager.py` | `collections.deque` | 最近 5 轮完整对话，超出后 LLM 摘要压缩 |
| 长期记忆 | `memory/long_term_memory.py` | SQLite + ChromaDB | 用户画像、历史行程、重要事实，跨 session 持久化 |
| 语义记忆 | `knowledge/rag_manager.py` | ChromaDB 向量库 | 目的地知识（景点/美食/住宿） |

长期记忆完整流向：

```
add_turn(user, assistant)
  └─→ LongTermMemory.extract_and_save()  # 正则提取实体，写 SQLite

get_messages(query=user_input)
  └─→ LongTermMemory.get_memory_context()  # 读取用户画像 + 历史行程
      └─→ 插入 messages[1] (system)  # ContextManager / ParseAgent / RecommendAgent 均调用

ParseAgent.execute()
  └─→ messages = [system_prompt, memory_system, human]  # LLM 感知用户历史偏好

RecommendAgent._recommend_destination/_recommend_time()
  └─→ system_prompt 内嵌 mem_context  # 推荐结果更个性化
```

---

### 3.5 工具层（`tools/`）

#### 三级降级策略

```
Level 1 — 真实 API（OpenWeather / 百度地图）
     ↓ 失败
Level 2 — 免费替代（DuckDuckGo 搜索 / Mock 数据）
     ↓ 失败
Level 3 — 浏览器自动化（browser-use + Playwright）
```

#### 工具清单

| 工具 | 路径 | 主要 API | 降级方案 |
|------|------|---------|---------|
| 天气查询 | `tools/utility/weather.py` | OpenWeather | Mock 数据 |
| 搜索 | `tools/utility/free_search.py` | DuckDuckGo（免费） | — |
| 地图路线 | `tools/utility/map_route.py` | 百度地图 MCP | DuckDuckGo → 通用建议 |
| 机票搜索 | `tools/transaction/flights.py` | DuckDuckGo | 携程/去哪儿链接 |
| 酒店搜索 | `tools/transaction/hotels.py` | DuckDuckGo | 携程/美团链接 |
| **平台攻略抓取** | `tools/content/platform_guides.py` | DuckDuckGo site过滤 | browser-use → 本地知识库 |
| 过敏原检测 | `tools/health/allergen_check.py` | 本地知识库（14类）| DuckDuckGo |
| 药物相互作用 | `tools/health/drug_interaction_check.py` | 本地规则（头孢/华法林等）| — |
| 食品安全 | `tools/health/food_safety_alert.py` | 本地规则 | — |

---

### 3.6 MCP 协议集成（`mcp_config.yaml`）

系统通过 Model Context Protocol 标准化工具调用：

| Server | 实现 | 功能 |
|--------|------|------|
| weather | `tools/mcp_servers/openweather_server.py` | 天气查询 |
| baidu_maps | `tools/mcp_servers/baidu_maps_server.py` | 地点搜索 / 路线 |
| brave_search | `@modelcontextprotocol/server-brave-search` | 网络搜索 |
| filesystem | `@modelcontextprotocol/server-filesystem` | 本地文件操作 |

MCP 客户端采用单例模式（`tools/mcp/mcp_client.py`），统一管理所有 Server 连接。

---

### 3.7 LLM 配置（`llm_config.py`）

统一使用 OpenAI 兼容接口，通过 `LLMConfig` 切换提供商：

| Provider | API 端点 | 默认模型 | 环境变量 |
|----------|---------|---------|---------|
| baidu_oneapi | `https://oneapi-comate.baidu-int.com/v1` | ERNIE-4.0-8K | `BAIDU_ONEAPI_KEY` |
| deepseek | `https://api.deepseek.com/v1` | deepseek-chat | `DEEPSEEK_API_KEY` |
| nvidia | `https://integrate.api.nvidia.com/v1` | llama-3.1-70b | `NVIDIA_API_KEY` |
| openai | `https://api.openai.com/v1` | gpt-4 | `OPENAI_API_KEY` |

---

## 4. 数据流

### 完整请求流程

```
用户："帮我规划5天云南旅行，预算8000元"
        │
        ▼
  ParseAgent（意图解析）
  → intent="plan_trip", destination="云南", duration=5, budget=8000
        │
        ├──→ RAG 检索（云南目的地知识）
        │
        ▼
  RecommendAgent（目的地推荐）
  → ["大理", "丽江", "香格里拉"] + 推荐理由
        │
        ▼
  PlanAgent（行程规划）
  → 逐日行程 JSON（景点/住宿/交通/预算分配）
        │
        ├──→ 调用天气工具（各目的地天气）
        ├──→ 调用地图工具（交通路线）
        └──→ 调用机票/酒店工具（价格参考）
        │
        ▼
  FoodAgent（美食推荐）
  → 特色餐厅 + 过敏原检测（基于用户 health_info）
        │
        ▼
  PsychologyAgent（健康评估）
  → 行程强度评分 + 休整建议
        │
        ▼
  MergeAgent（内容融合）
  → 整合所有 Agent 输出为统一结构
        │
        ▼
  OutputAgent（报告生成）
  ├──→ output/reports/{id}.md（客户 Markdown 报告）
  └──→ output/social/{id}.json（自媒体素材）
```

---

## 5. 技术栈

| 类别 | 技术选型 | 版本 |
|------|---------|------|
| Agent 编排 | LangGraph | >=0.2.0 |
| LLM 框架 | LangChain + LangChain-OpenAI | >=1.3.0 |
| 向量数据库 | ChromaDB | >=0.4.24 |
| Embedding 模型 | BAAI/bge-small-zh-v1.5 | via sentence-transformers >=2.7.0 |
| BM25 检索 | rank-bm25 | >=0.2.2 |
| Web UI | Streamlit | >=1.33.0 |
| 数据验证 | Pydantic | >=2.7.0 |
| 浏览器自动化 | browser-use + Playwright | >=0.1.10 / >=1.44.0 |
| 测试框架 | pytest + pytest-asyncio | >=8.2.0 |

---

## 6. 关键设计决策

### 6.1 双模式并存
AgentManager 轻量模式供 Web UI 使用，支持灵活的单 Agent 调用；LangGraph 模式提供标准化图编排，适合生产部署。两者共用相同的 Agent 实现（BaseAgent 基类），避免代码重复。

### 6.2 混合检索 vs 纯向量检索
纯向量检索对目的地名称等实体词召回不稳定，引入 BM25 关键词匹配补充，通过 RRF 融合使两者互补。权重配置（向量 0.6 / BM25 0.4）可调。

### 6.3 三级工具降级
旅游数据 API 多为收费或有调用限制，通过降级到 DuckDuckGo 免费搜索和浏览器自动化兜底，保证系统在无 API Key 情况下仍可运行。

### 6.4 OpenAI 兼容接口统一 LLM 调用
不绑定特定 LLM 提供商，通过环境变量切换，降低迁移成本，支持国内模型（文心、DeepSeek）和海外模型（GPT-4、Llama）。

### 6.5 上下文压缩双层策略
LangGraph 图编排层（`state.py`）用 `_keep_latest_messages` 截断消息列表，防止单次 LLM 调用超限；跨轮次层（`context_manager.py`）用 LLM 摘要压缩保留语义，两层互相补充，在 token 受限环境下支持无限轮对话。

### 6.6 药物-食物相互作用检测
旅行场景中用户可能携带常用药，头孢类+酒精等组合存在致命风险（双硫仑样反应）。内置 `drug_interaction_check.py`，无需外部 API，完全本地运行，与过敏原检测协同使用。

### 6.7 Agent Loop 与固定 Pipeline 共存
未用 ReAct 全盘替换固定 Pipeline：简单意图（如"三亚几月去"）走固定 Pipeline，延迟低、token 省、结果可预测；复杂开放式查询（多目的地对比、多约束）走 `ReActAgent` 的 Think-Act-Observe 循环，工具调用由推理动态驱动。两者由意图复杂度路由，兼顾效率与灵活性。

### 6.8 反思自纠错的非侵入设计
`ReflectionMixin` 以混入方式让任意 Agent 获得"执行→验证→重试"能力，验证器分规则式（零成本、毫秒级）和 LLM-as-Judge（高质量、有成本）两级；LangGraph 用回环边实现，`MAX_REFLECTION_RETRIES=2` 防死循环。反思逻辑与业务逻辑解耦。

### 6.9 安全护栏的中间件化
Guardrails 不侵入任何 Agent，只在 `run_pipeline` 入口/出口做前置/后置拦截，可通过 `enable_guardrails=False` 一键关闭。输入侧防注入+脱敏，输出侧防幻觉+标记置信度，配合 BudgetLimiter 限流。

### 6.10 长期记忆的拟人化管理
不止 KV 存储：冲突检测（矛盾偏好提醒而非静默覆盖）、时间衰减+LRU+容量上限（模拟遗忘）、动态重要性评分（强调加权、负偏好高分）、LLM 辅助提取隐含偏好。四者使记忆更贴近人类认知。

---

## 7. 目录结构

```
AI_Agent_Travel_System/
├── main.py                    # LangGraph 图编排入口（含 Reflection 回环边）
├── agent_manager.py           # AgentManager（护栏/流式/HITL/tracing/run_react）
├── context_manager.py         # 多轮对话上下文压缩（滑动窗口 + LLM 摘要）
├── state.py                   # AgentState 类型定义（含 reflection/react 字段）
├── config.py / llm_config.py  # 配置管理
├── async_executor.py          # 异步并行执行器 ⭐
├── streaming.py               # 流式输出回调（观察者模式）⭐
├── rag_memory_system.py       # RAG + Memory 设计文档
├── mcp_config.yaml            # MCP 服务器配置
├── memory/                    # 长期记忆模块 ⭐
│   ├── long_term_memory.py    # SQLite + ChromaDB + 冲突/衰减/动态重要性/LLM提取
│   └── data/                  # 用户记忆数据库（.db 文件）
├── guardrails/                # 安全护栏（中间件）⭐
│   ├── input_guard.py         # 注入检测 + PII 脱敏 + 内容安全
│   ├── output_guard.py        # 幻觉检测 + PII 泄露 + 置信度
│   ├── budget_limiter.py      # token/请求/工具调用限流
│   └── config.py              # 护栏配置
├── evaluation/                # 评估框架 ⭐
│   ├── dataset.py / evaluator.py / metrics.py / judges.py / run_eval.py
│   └── test_cases/            # 20 个预置测试用例
├── observability/             # 可观测性 ⭐
│   └── tracer.py              # Trace/Span 调用链 + 指标 + 导出
├── planning/                  # 动态规划 ⭐
│   └── planner.py             # Plan-and-Execute 分解 + 重规划
├── agents/                    # 13 个 Agent 实现
│   ├── reflection.py          # 反思自纠错（Mixin + 验证器 + LLMJudge）⭐
│   ├── react_agent.py         # ReAct 循环 Agent ⭐
│   └── ...                    # base/parse/recommend/plan/travel/food/...
├── tools/
│   ├── utility/               # 天气、搜索、地图
│   ├── content/               # 攻略、风俗、特色；platform_guides 四平台抓取
│   ├── transaction/           # 机票、酒店、门票
│   ├── health/                # 过敏原、食品安全、药物相互作用
│   └── mcp_servers/           # MCP Server 实现
├── knowledge/
│   ├── rag_manager.py         # RAG 管理器（ChromaDB）
│   ├── retriever.py           # 混合检索器（向量 + BM25）
│   ├── raw_data/              # 目的地原始知识库
│   └── vector_db/             # ChromaDB 持久化
├── web/
│   └── app_v2.py              # Streamlit Web UI
├── output/
│   ├── reports/               # 生成的客户报告（Markdown）
│   └── social/                # 自媒体素材（JSON）
└── tests/                     # 测试套件
```
