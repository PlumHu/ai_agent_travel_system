# 快速启动指南

> **10 分钟让你的 AI Travel Agent 跑起来**
> 版本：v1.2 | 更新时间：2026-07-05

---

## ⚡ 最快启动（4 步）

### 1️⃣ 安装依赖

```bash
cd AI_Agent_Travel_System
pip install -r requirements.txt
```

### 2️⃣ 配置 API Key

```bash
cp .env.example .env
# 编辑 .env，填入至少一个 LLM 的 API Key（必填）
```

### 3️⃣ 构建 RAG 索引（首次，约 5-10 分钟）

```bash
python3 knowledge/build_index.py --sample
```

> 首次运行会从 HuggingFace 下载 Embedding 模型（约 100MB）。
> 下载慢可设置镜像：`export HF_ENDPOINT=https://hf-mirror.com`

### 4️⃣ 运行项目

```bash
# 方式一：Web 界面（推荐）
streamlit run web/app_v2.py

# 方式二：命令行
python3 main.py
```

---

## 📝 详细步骤

### 环境要求

- Python 3.10+
- 至少 4GB RAM（RAG Embedding 模型需要）
- 至少一个 LLM API Key

---

### 步骤 1：安装依赖

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

`requirements.txt` 已更新为宽松约束（`>=`），避免固定版本冲突。如遇问题：

```bash
# Embedding 模型下载慢
pip install sentence-transformers -i https://pypi.tuna.tsinghua.edu.cn/simple

# ChromaDB 安装失败
pip install --upgrade setuptools wheel
pip install chromadb
```

---

### 步骤 2：配置环境变量

`.env` 文件已存在（无需 cp）。打开后按需修改：

**【必填】选择一个 LLM 提供商（改两行）：**

```env
# 方案 A：DeepSeek（推荐，便宜，效果接近 GPT-4）
DEFAULT_LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-你的key        # 取消注释 + 填入
# 申请：https://platform.deepseek.com/api_keys

# 方案 B：OpenAI
DEFAULT_LLM_PROVIDER=openai
OPENAI_API_KEY=sk-你的key          # 已有行，直接替换 sk-xxx

# 方案 C：英伟达 NIM（有免费额度）
DEFAULT_LLM_PROVIDER=nvidia
NVIDIA_API_KEY=nvapi-你的key       # 取消注释 + 填入
# 申请：https://build.nvidia.com/
```

config.py 会按 `DEFAULT_LLM_PROVIDER` 自动路由，**只需改这两行**，无需手动改 `OPENAI_API_BASE` 等。

**【可选】工具 API（不填自动降级，不影响主流程）：**

| 工具 | 环境变量 | 不填时降级到 | 申请地址 |
|------|---------|------------|---------|
| 天气查询 | `OPENWEATHER_API_KEY` | Mock 数据 | openweathermap.org → Free Plan |
| 路线规划 | `BAIDU_MAPS_API_KEY` | DuckDuckGo 搜索 | lbsyun.baidu.com |
| 搜索增强 | `BRAVE_SEARCH_API_KEY` | DuckDuckGo 免费 | api.search.brave.com（2000次/月免费）|
| 搜索增强 | `SERPER_API_KEY` | DuckDuckGo 免费 | serper.dev（2500次免费）|

全部不填也能正常运行，只是天气用 Mock、搜索用 DuckDuckGo。

---

### 步骤 3：构建 RAG 知识库索引

**RAG 原理**：
- HuggingFace `BAAI/bge-small-zh-v1.5` 负责把文字翻译成语义向量（Embedding 模型）
- ChromaDB 负责存储这些向量并提供相似度检索（向量数据库）
- 两者合作实现"输入查询 → 检索相关目的地知识 → 传给 LLM"

```bash
# 使用内置示例数据（大理/丽江/三亚）
python3 knowledge/build_index.py --sample

# 或从 raw_data 全量构建
python3 knowledge/build_index.py --data-dir knowledge/raw_data
```

构建成功后，向量索引保存在 `knowledge/vector_db/`，后续启动无需重新构建。

---

### 步骤 4：运行项目

**Web UI（推荐）**：
```bash
streamlit run web/app_v2.py
# 浏览器打开 http://localhost:8501
```

**命令行**：
```bash
python3 main.py
```

**代码调用（多轮对话）**：
```python
from agent_manager import AgentManager

manager = AgentManager()

# 支持多轮对话，自动管理上下文
r1 = manager.run_pipeline("我想去大理，预算5000")
r2 = manager.run_pipeline("有什么美食推荐？")   # 上下文中记住了大理

# 查看上下文压缩状态
print(manager.ctx.get_stats())

# 开始新会话
manager.reset_context()
```

---

## ✅ 验证安装

```python
# 验证 RAG
from knowledge.rag_manager import RAGManager
rag = RAGManager()
docs = rag.retrieve("海边旅游")
print(docs[0]["metadata"].get("destination", "ok"))  # 应输出: 三亚

# 验证天气工具
from tools.utility.weather import get_weather
print(get_weather("大理"))

# 验证健康工具（无需 API）
from tools.health.drug_interaction_check import check_drug_interactions
result = check_drug_interactions(medications=["头孢克洛"], planned_foods=["啤酒鸭"])
print(result)  # 应输出危险警告

# 验证平台攻略抓取（DuckDuckGo 可用时返回在线结果，否则本地兜底）
from tools.content.platform_guides import search_platform_guides
result = search_platform_guides("大理", use_browser_fallback=False)
import json; data = json.loads(result)
print(f"攻略抓取: {data['total_results']} 条结果")  # DuckDuckGo 可用时 ≥4 条

# 验证上下文管理器
from context_manager import ContextManager
ctx = ContextManager()
ctx.add_turn("我想去大理", "好的，大理是个好地方")
print(ctx.get_stats())  # {'history_turns': 1, ...}
```

---

## 🐛 常见问题

### Q1：Embedding 模型下载失败或太慢？

```bash
export HF_ENDPOINT=https://hf-mirror.com
python3 knowledge/build_index.py --sample
```

或手动下载后指向本地路径（修改 `.env`）：
```env
EMBEDDING_MODEL=./knowledge/bge-small-zh-v1.5
```

### Q2：`ModuleNotFoundError: No module named 'langchain'`

```bash
source venv/bin/activate   # 确认虚拟环境已激活
pip install -r requirements.txt
```

### Q3：API Key 报错

检查 `.env` 是否存在且 Key 填写正确：
```bash
cat .env | grep OPENAI_API_KEY
```

### Q4：DuckDuckGo 搜索失败

网络问题，系统自动降级到 Mock 数据。可配置 `BRAVE_SEARCH_API_KEY` 作为备用。

### Q5：Streamlit 界面空白或端口冲突

```bash
rm -rf ~/.streamlit/
streamlit run web/app_v2.py --server.port 8502
```

### Q6：多轮对话 token 超限？

项目已内置双层上下文压缩：
- AgentState 层：消息列表超过 20 条自动截断
- ContextManager 层：估算超过 6000 token 自动 LLM 摘要压缩

无需手动处理，`AgentManager.run_pipeline()` 会自动管理。

---

## 🎯 快速测试用例

```
# 测试 1：行程规划
我想6月中旬去大理玩5天，预算5000元，喜欢吃美食和看风景

# 测试 2：目的地推荐
推荐一个适合周末放松的海边城市

# 测试 3：反向推荐（推荐时间）
三亚什么时候去最合适？人少一点的时候

# 测试 4：复杂需求
我和女朋友想去云南度蜜月，7天，预算1万，喜欢安静，不要太商业化
```

---

## 🔗 下一步

- 阅读 [README.md](README.md) 了解完整架构
- 阅读 [系统设计文档.md](系统设计文档.md) 了解上下文压缩机制
- 查看 [agents/](agents/) 目录学习 Agent 实现
- 添加新目的地数据到 [knowledge/raw_data/destinations/](knowledge/raw_data/destinations/)


> **10 分钟让你的 AI Travel Agent 跑起来**

---

## ⚡ 最快启动（3 步）

### 1️⃣ 安装依赖

```bash
cd travel_system
pip install -r requirements.txt
```

### 2️⃣ 配置 API Key

```bash
cp .env.example .env
# 编辑 .env 文件，填入你的 OpenAI API Key
echo "OPENAI_API_KEY=sk-your-key-here" > .env
```

### 3️⃣ 运行项目

```bash
# 方式一：命令行模式
python main.py

# 方式二：Web 界面
streamlit run web/app.py
```

---

## 📝 详细步骤

### 环境要求

- Python 3.10+
- 至少 4GB RAM
- OpenAI API Key（或兼容的国内服务）

---

### 步骤 1：克隆/下载项目

```bash
# 如果是 Git 仓库
git clone https://github.com/yourusername/travel_system.git
cd travel_system

# 如果是压缩包
unzip travel_system.zip
cd travel_system
```

---

### 步骤 2：创建虚拟环境（推荐）

```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# macOS/Linux:
source venv/bin/activate

# Windows:
venv\Scripts\activate
```

---

### 步骤 3：安装依赖

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

**预期输出：**
```
Successfully installed langchain-0.1.20 langgraph-0.0.55 chromadb-0.4.24 ...
```

**如果遇到错误：**
```bash
# 如果 ChromaDB 安装失败
pip install --upgrade setuptools wheel
pip install chromadb==0.4.24

# 如果 SentenceTransformer 下载慢
pip install sentence-transformers -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

### 步骤 4：配置环境变量

```bash
# 复制模板
cp .env.example .env

# 编辑 .env 文件
nano .env  # 或使用 vim, vscode 等
```

**.env 文件内容：**
```env
# OpenAI 配置（必填）
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_MODEL=gpt-4-turbo-preview

# 可选：使用国内服务
# OPENAI_API_KEY=your-deepseek-key
# OPENAI_API_BASE=https://api.deepseek.com/v1
# OPENAI_MODEL=deepseek-chat

# 其他 API（可选）
# SERPER_API_KEY=your-serper-key
# BRAVE_SEARCH_API_KEY=your-brave-key
# OPENWEATHER_API_KEY=your-weather-key
```

**保存并退出**（nano: Ctrl+X, Y, Enter）

---

### 步骤 5：构建知识库索引

```bash
# 首次运行需要构建向量索引
python -c "from knowledge.rag_manager import RAGManager; rag = RAGManager(); rag.build_index(force_rebuild=True)"
```

**预期输出：**
```
INFO:rag_manager:加载 Embedding 模型: BAAI/bge-small-zh-v1.5
INFO:rag_manager:创建新集合: travel_knowledge
INFO:rag_manager:加载文档: dali.json
INFO:rag_manager:加载文档: sanya.json
INFO:rag_manager:索引构建完成，共 2 条文档
```

**注意：** 第一次运行会下载 Embedding 模型（约 100MB），可能需要 5-10 分钟。

---

### 步骤 6：运行测试

```bash
# 测试核心功能
python main.py
```

**交互示例：**
```
🧳 欢迎使用 AI Travel Agent (旅行规划助手)
============================================================

请输入您的旅行需求（输入 'quit' 退出）:
> 我想6月去大理玩5天，预算5000元

[Parse Agent] 开始解析用户需求
用户输入: 我想6月去大理玩5天，预算5000元
解析结果:
  - 目的地: 大理
  - 日期: 2026-06-01 ~ 2026-06-05
  - 预算: 5000
  - 偏好: []
  - 意图: plan_trip

[Travel Agent] 开始规划旅行
检索目的地知识: 大理
检索到 2 条相关文档
旅行计划生成成功

[Output Agent] 生成最终报告
报告已保存: output/reports/大理_旅行计划_20260601_143022.md

✅ 旅行计划已生成！
📅 行程天数: 5 天
💰 预算总计: 5000 元
🍜 美食推荐: 5 项
```

**输入 `quit` 退出**

---

### 步骤 7：启动 Web 界面

```bash
streamlit run web/app.py
```

**预期输出：**
```
You can now view your Streamlit app in your browser.

  Local URL: http://localhost:8501
  Network URL: http://192.168.1.100:8501
```

**浏览器会自动打开 `http://localhost:8501`**

---

## ✅ 验证安装

### 测试 1：RAG 检索

```python
from knowledge.rag_manager import RAGManager

rag = RAGManager()
results = rag.retrieve("我想去海边")
print(results[0]["metadata"]["destination"])  # 应该输出: 三亚
```

---

### 测试 2：Tools 调用

```python
from tools.utility.weather import get_weather

weather = get_weather("大理")
print(weather)  # 应该输出 JSON 格式的天气信息
```

---

### 测试 3：Parse Agent

```python
from agents.parse_agent import parse_agent
from state import create_initial_state

state = create_initial_state("我想去大理玩")
result = parse_agent(state)
print(result["destination"])  # 应该输出: 大理
```

---

## 🐛 常见问题排查

### 问题 1：`ModuleNotFoundError: No module named 'langchain'`

**原因：** 未安装依赖或虚拟环境未激活

**解决：**
```bash
# 确认虚拟环境已激活（命令行前有 (venv) 标记）
source venv/bin/activate  # macOS/Linux
venv\Scripts\activate     # Windows

# 重新安装依赖
pip install -r requirements.txt
```

---

### 问题 2：`OpenAI API Key not found`

**原因：** 未配置 `.env` 文件或未加载

**解决：**
```bash
# 检查 .env 文件是否存在
ls -la .env

# 确认 OPENAI_API_KEY 已设置
cat .env | grep OPENAI_API_KEY

# 如果没有，手动创建
echo "OPENAI_API_KEY=sk-your-key" > .env
```

---

### 问题 3：`ChromaDB: Could not find a version...`

**原因：** pip 版本过低或系统兼容性问题

**解决：**
```bash
# 升级 pip 和 setuptools
pip install --upgrade pip setuptools wheel

# 指定版本安装
pip install chromadb==0.4.24
```

---

### 问题 4：Embedding 模型下载失败

**原因：** 网络问题或 Hugging Face 访问受限

**解决方案 1：使用国内镜像**
```python
# 修改 config.py
EMBEDDING_MODEL = "BAAI/bge-small-zh-v1.5"
os.environ["HF_ENDPOINT"] = "https://hf-mirror.com"
```

**解决方案 2：手动下载**
```bash
# 下载模型到本地
cd knowledge/
git clone https://huggingface.co/BAAI/bge-small-zh-v1.5

# 修改 config.py
EMBEDDING_MODEL = "./knowledge/bge-small-zh-v1.5"
```

---

### 问题 5：Streamlit 界面空白

**原因：** 浏览器缓存或端口占用

**解决：**
```bash
# 清除 Streamlit 缓存
rm -rf ~/.streamlit/

# 指定其他端口
streamlit run web/app.py --server.port 8502
```

---

## 🎯 快速测试用例

复制以下测试用例到命令行或 Web 界面：

### 测试 1：基础功能
```
我想6月中旬去大理玩5天，预算5000元，喜欢吃美食和看风景
```

**预期结果：**
- 正确解析目的地、日期、预算
- 生成 5 天详细行程
- 包含美食推荐和预算分解

---

### 测试 2：目的地推荐
```
推荐一个适合周末放松的海边城市
```

**预期结果：**
- 推荐三亚
- 给出海滩、住宿、美食建议

---

### 测试 3：复杂需求
```
我和女朋友想去云南度蜜月，7天时间，预算1万元，喜欢安静的地方，不要太商业化
```

**预期结果：**
- 推荐大理或丽江
- 行程偏向自然风光和文化体验
- 避开商业化景点

---

## 📦 一键部署脚本

创建 `quick_start.sh`（macOS/Linux）：

```bash
#!/bin/bash

echo "🚀 开始部署 AI Travel Agent..."

# 1. 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 2. 安装依赖
pip install --upgrade pip
pip install -r requirements.txt

# 3. 配置环境变量
if [ ! -f .env ]; then
    cp .env.example .env
    echo "⚠️  请编辑 .env 文件，填入 OpenAI API Key"
    exit 1
fi

# 4. 构建索引
python -c "from knowledge.rag_manager import RAGManager; rag = RAGManager(); rag.build_index(force_rebuild=True)"

# 5. 运行测试
python main.py

echo "✅ 部署完成！运行 'streamlit run web/app.py' 启动 Web 界面"
```

**使用方法：**
```bash
chmod +x quick_start.sh
./quick_start.sh
```

---

## 🔗 下一步

- [ ] 阅读 [README.md](README.md) 了解项目架构
- [ ] 阅读 [面试准备手册.md](面试准备手册.md) 准备面试话术
- [ ] 查看 [agents/](agents/) 目录学习 Agent 实现
- [ ] 尝试添加新的目的地数据到 [knowledge/raw_data/](knowledge/raw_data/)
- [ ] 修改 [config.py](config.py) 调整参数

---

## 🆘 获取帮助

- 查看项目文档：`README.md`
- 提交 Issue：GitHub Issues
- 加入讨论：GitHub Discussions
- 联系作者：your.email@example.com

---

**祝你使用愉快！如有问题随时联系 📧**
